export default function removeSession(){
    // Remove the current session for user that just logged out
        // Remove the sessionId from the server
        // May need to remove the sessionStorage, if used
    return;
}