//This function will be called to send message to database
export default function sendMessage(msg){
    //Verify session
    //Update the messages store with new message
    //Update the message on the server and
    //return the status of the server response to shown unsend Message
    return;
}