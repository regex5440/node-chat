export default function verifySession(){
    // This function will verify if the user has already logged in 
    // This function is supposed to be performed whenever there is a action updating server DB
    return;
}